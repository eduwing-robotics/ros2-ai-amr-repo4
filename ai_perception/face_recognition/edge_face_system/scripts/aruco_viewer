#!/usr/bin/env bash
set -eo pipefail
if [ -f /opt/ros/jazzy/setup.bash ]; then
  source /opt/ros/jazzy/setup.bash
fi
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"
cd "$PROJECT_DIR"
PYTHONPATH="$PROJECT_DIR:${PYTHONPATH:-}" "$HOME/face-env/bin/python" -m pc_side.image_viewer \
  --topic "${ARUCO_VIEWER_TOPIC:-/aruco/annotated_image}" \
  --window-name "${ARUCO_VIEWER_WINDOW:-ArUco-Viewer}" \
  --max-width "${ARUCO_VIEWER_MAX_WIDTH:-960}" \
  "$@"
